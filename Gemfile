# Minimal Gemfile to satisfy Bundler when IRuby starts under Jupyter.
# Declare IRuby to avoid warnings when Bundler loads under the kernel.
source "https://rubygems.org"

# Keep only iruby here to prevent conflicts with globally installed gems.
gem 'iruby', '~> 0.8.2'
